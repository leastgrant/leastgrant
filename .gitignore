node_modules/
dist/
*.tsbuildinfo
.DS_Store
.leastgrant/
*.log
coverage/
.env
.env.*

# Scratch from the build process, not part of the project
.workflows/
.research/
.audit/

# Local `npm pack` output. Never commit a build artifact.
*.tgz

# Website scratch. The capture sandbox is a throwaway home directory the site
# build creates to run the real CLI in; the rest are design-iteration renders.
site/.capture/
site/.logo-*
site/.brand-preview.*

# Worktrees created by background agents. Never part of the repository.
.claude/worktrees/
